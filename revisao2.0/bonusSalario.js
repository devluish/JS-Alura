// 💰 7. Cálculo de salário com bônus
// Crie uma função que recebe o salário de um funcionário e se ele bateu a meta (true ou false). Se bateu:

// Acrescenta 20% de bônus.
// Se não, o salário permanece igual.
// Retorne o valor final.


const salario = 2500;

const pagamento = (salario) =>{
    const valorMeta = salario + (20 / 100 * salario);
    const 

    if (    ) {
        
    }
}